npm和npx的区别：
npx的作用是先检查本地有没有安装某个package，如果没有去远程registry找，
找到的话直接使用，不用下载到本地node-modules包里面，这样就能优化本地项目的大小，
也可以避免安装package到全局。

为什么用 google font：
网站可能要用到用户终端设备没有安装的字体，也为了给所有用户统一的体验
导入时使用link可以让浏览器预处理，加快网页加载速度。也可以使用@import

powershell 找不到命令，可能是因为用户的path中没有包含该程序配置，需要将系统的path拷贝过去
